# Core API & server
fastapi
uvicorn[standard]
pydantic

# HTTP client (used to call Ollama)
httpx

# LlamaIndex - core + HuggingFace embeddings only
# NOTE: llama-index-llms-ollama intentionally excluded —
# Ollama and Gemini are both called directly via httpx for provider flexibility.
llama-index-core
llama-index-embeddings-huggingface

# HuggingFace sentence-transformers (local embeddings - no API key needed)
sentence-transformers
torch

# PDF reading
pypdf

# Misc
python-dotenv
