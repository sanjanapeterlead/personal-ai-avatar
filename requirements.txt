# Core API & server
fastapi
uvicorn[standard]
pydantic

# HTTP client (used to call Ollama)
httpx

# LlamaIndex - core + HuggingFace embeddings only
# NOTE: llama-index-llms-ollama intentionally excluded —
# Ollama and Gemini are both called directly via httpx for provider flexibility.
llama-index-core
llama-index-embeddings-huggingface

# HuggingFace sentence-transformers (local embeddings - no API key needed)
sentence-transformers
torch

# PDF reading — llama-index-readers-file provides the PDFReader that
# SimpleDirectoryReader uses for .pdf files; without it, PDFs get read as raw
# bytes instead of extracted text (see project-issues-faced-and-solved.md, Issue 3).
pypdf
llama-index-readers-file

# Misc
python-dotenv
